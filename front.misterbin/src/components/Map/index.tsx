import styled from "styled-components";

const Map: React.FC = () => {
  return (
    <Container>
      Ce sera la page de la map.
    </Container>
  );
};

const Container = styled.div``;

export default Map;